# Телеграм-бот «Трекер привычек»

## 1. Назначение проекта
описание проекта
Проект представляет собой Telegram-бота «Трекер привычек», предназначенного для помощи пользователям в формировании и поддержании полезных привычек. Бот позволяет добавлять собственные привычки, отмечать их выполнение, просматривать статистику и получать рекомендации по саморазвитию. Система использует базу данных SQLite для хранения информации о пользователях и их привычках, а также интеграцию с ИИ-моделью для генерации персонализированных советов. Проект реализован на Python с использованием фреймворка aiogram 3, что обеспечивает стабильную и современную архитектуру бота.

Цель проекта — реализовать простого Telegram-бота для отслеживания привычек.  
Пользователь может:

- добавлять свои привычки;
- просматривать их список;
- отмечать выполнение привычек;
- смотреть простую статистику по выполнению;
- получать краткие рекомендации от ИИ по формированию полезных привычек.

Бот рассчитан на личное использование: запуск на локальной машине/сервере и общение через Telegram.

---

## 2. Технологический стек

- Язык программирования: **Python 3.11+**
- Фреймворк для Telegram: **aiogram 3**
- База данных: **SQLite** (легковесная, файл на диске)
- Работа с переменными окружения: `python-dotenv`
- Интеграция с ИИ (опционально): `openai` (новый клиент `OpenAI`)

Файл настроек: `config/settings.py`  
Файл с точкой входа: `main.py`

---

## 3. Архитектура и структура проекта

Основные директории и файлы:
- `main.py` — точка входа. Настраивает бота и диспетчер, запускает polling.
- `bot/handlers.py` — обработчики команд и сообщений пользователя (aiogram 3, `Router`).
- `database/manager.py` — работа с БД (SQLite): создание таблиц, добавление пользователей/привычек, отметок, статистика.
- `ai/agent.py` — модуль для общения с ИИ (OpenAI). При отсутствии ключа даёт безопасный «заглушечный» ответ.
- `config/settings.py` — загрузка переменных окружения из `.env`.
- `models/` — dataclass-модели, описывающие пользователя и привычку.


Логика разделена следующим образом:
1. **Вход в бот** (`/start`) — регистрация пользователя в БД и показ главного меню.
2. **Добавление привычки** — сценарий с несколькими шагами через FSM:
   - бот спрашивает название привычки;
   - затем периодичность в свободной форме.
3. **Просмотр списка привычек** — вывод всех привычек пользователя.
4. **Отметка выполнения привычки** — пользователь выбирает привычку и бот фиксирует выполнение за сегодня.
5. **Статистика** — простая статистика по количеству отметок для каждой привычки.
6. **Совет от ИИ** — короткая подсказка по формированию привычек (если доступен OpenAI).

---

## 4. Структура базы данных
Используется один файл БД (путь задаётся в `.env` переменной `DATABASE_PATH`).
Основные таблицы:
1. **users**

   - `user_id` — целое, первичный ключ (Telegram ID пользователя).
   - `username` — логин Telegram (может быть `NULL`).
   - `first_name` — имя пользователя.
   - `created_at` — дата/время регистрации в боте (ISO-строка).
2. **habits**

   - `id` — целое, первичный ключ, AUTOINCREMENT.
   - `user_id` — внешний ключ на `users.user_id`.
   - `name` — название привычки.
   - `period` — периодичность (строка в свободной форме).
   - `created_at` — дата/время создания привычки.
3. **entries**

   - `id` — целое, первичный ключ, AUTOINCREMENT.
   - `habit_id` — внешний ключ на `habits.id`.
   - `date` — дата выполнения (строка формата `YYYY-MM-DD`).
   - `done` — флаг выполнения (0 или 1, сейчас используется как «выполнено»).
   - `note` — необязательное поле для заметок (пока логически не используется в интерфейсе).
   - `created_at` — дата/время создания записи.

Таблицы создаются автоматически при первом запуске бота через функцию `init_db()` в `database/manager.py`.

---

## 5. Основные сценарии работы с ботом

### 5.1. Старт и помощь
- Команда `/start`:
  - регистрирует пользователя в таблице `users`;
  - показывает главное меню с кнопками.
- Команда `/help`:
  - выводит краткую справку по доступным функциям.

### 5.2. Добавление новой привычки

Через кнопку **«➕ Добавить привычку»**:
1. Бот спрашивает: «Какую привычку хотите отслеживать?».
2. Пользователь вводит краткое название (например, «Пить воду», «Читать 10 страниц»).
3. Бот спрашивает о периодичности (например, «каждый день», «3 раза в неделю»).
4. Создаётся запись в таблице `habits`.

### 5.3. Просмотр списка привычек

Через кнопку **«📋 Мои привычки»**:
- Бот выводит список сохранённых привычек вида:
Если привычек ещё нет — бот предлагает добавить первую.

### 5.4. Отметка выполнения привычки

Через кнопку **«✅ Отметить выполнение»** или команду `/done`:
1. Бот выводит нумерованный список привычек пользователя:
2. Пользователь отправляет номер (например, `1`).
3. Бот сохраняет запись в таблицу `entries` с текущей датой и `done = 1`.
4. Бот подтверждает: «Отметил выполнение привычки "Пить воду" за сегодня.»

### 5.5. Статистика

Через кнопку **«📊 Статистика»** или команду `/stats`:
- Бот считает количество записей в `entriesэ для каждой привычки пользователя.
- Выводит примерный вид:


